env
# API Settings
API_KEY=your-static-token
API_PREFIX=/api/v1

# Redis Settings
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# Scraper Settings
BASE_URL=http://localhost:5000/health
MAX_RETRIES=3
RETRY_DELAY=5

# Storage Settings
STORAGE_PATH=/app/data/test.json
IMAGES_DIR=/app/data/images

# Proxy Settings (optional)
DEFAULT_PROXY=